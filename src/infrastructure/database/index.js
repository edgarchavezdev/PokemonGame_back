const { inicializarFirestore, obtenerFirestore } = require('./firestore');
const FirestoreRepositorioBase = require('./firestore-repositorio.base');

module.exports = {
  inicializarFirestore,
  obtenerFirestore,
  FirestoreRepositorioBase,
};
